#!/usr/bin/env bash

if [ "$ROFI" ]
then
    alacritty -e "zsh" -c "cd ~/Documents/org/ && nvim refile.org --startuptime startuptime"
else
    cd ~/Documents/org/ && nvim refile.org --startuptime startuptime
fi

