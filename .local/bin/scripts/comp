#!/bin/env bash

if [[ $# -eq 1 ]]
then
    output=$(echo $1 | awk 'BEGIN{FS=OFS="."}{NF--; print}')
    gcc -Wall -std=gnu99 -g -o $output $1
    # ./$output
else
    echo "Please only use one argument"
fi
