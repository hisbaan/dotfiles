require('live-command').setup({
    commands = {
        Norm = { cmd = "norm" }
    },
    -- defaults = {
    --     hl_groups = {
    --         insertion = "DiffChange",
    --         deletion = "DiffChange",
    --         change = "DiffChange",
    --     }
    -- }
})
